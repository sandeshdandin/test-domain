version: "3.8"

services:
  nginx:
    image: nginx:latest
    container_name: hello-nginx
    ports:
      - "1234:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped